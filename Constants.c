#include <stdio.h>

int main()
{
    
     // A constant is supposed to be a fixed value.
     //Example

    const float PI= 3.14; 
     
     // preceede with const then make the name upper case

     printf("%.2f",PI);
    
    return 0;
}